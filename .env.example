# Grok CLI Environment Variables

# API Configuration
GROK_API_KEY=your-api-key-here

# 🛡️ WATCHER DAEMON AUTO-START
# Enable auto-start of watcher daemon when grokinou starts
# CRITICAL: Détection "à rebours" - fichiers altérés AVANT lancement seront détectés
GROK_AUTO_WATCHER=true

# Watcher mode: heuristic, dual, llm
GROK_WATCHER_MODE=dual

# Enable/disable LLM Guard (default: true)
GROK_LLM_GUARD=true

# LLM Guard API Key (defaults to GROK_API_KEY if not set)
# LLM_GUARD_API_KEY=your-llm-guard-api-key

# 🔧 Continuous Self-Integrity Monitoring (default: true)
GROK_CONTINUOUS_SELF_INTEGRITY=true

# Self-integrity check interval in milliseconds (default: 10000 = 10s)
GROK_SELF_INTEGRITY_INTERVAL=10000
